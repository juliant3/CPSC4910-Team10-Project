"""
- validate_registration_payload(data)
- validate_points_payload(data)          (amount is int != 0, reason is non-empty string)
- validate_date_range(start, end)        (used by every report endpoint)
- validate_email(email)
"""
